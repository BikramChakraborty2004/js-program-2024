const id = 987;
let age = 18;
var name = "Bikram";
state = "student"
let temperature = null // Thats means Temperature variable is empty ** It does not mean ZERO
let variable;  // If we not initialise any variable it will be undefined 
// id=900  // const variable data must not be change

/*
var keyword should not be use due to block scope
*/

console.log(id)
console.log(name);
console.table([id,age,name,state]);

